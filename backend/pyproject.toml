[project]
name = "drone-rl"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "numpy",
    "numpy-quaternion",
]

[project.optional-dependencies]
dev = [
    "ruff"
]

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "B"]